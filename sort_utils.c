#include "push_swap.h"

/*
void ft_sort(t_stack **a, t_stack **b)
{
	int size;

	size = ft_stksize(*a);
	while (*a && size > 0)
	{
		if ((*a)->content >= 0)
		{
			push('b', a, b);
		}
		else
			rot('a', a);
		size --;
	}
}
*/
